export type globalResponse = {
    success: boolean;
    message: string;
}

export type user = {
    username: string;
    email: string;
    token: string;
}

export type userFormData = {
    username: string;
    email: string;
    password: string;
}
